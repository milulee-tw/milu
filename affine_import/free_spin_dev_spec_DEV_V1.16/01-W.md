# 🖼 01-W · Walkthrough 畫布 · 後台操作流程

> 依據：規格 DEV V1.16（源自 DEV V1.15）／ 後台原型 2026-08-25 實拍 ／ 背包原型 2026-09-14 新版線框
> 職能：📋 企劃&PM · 🖥 Web 前端

## 一、一句話

兩條後台流程的畫布底稿：型錄複用（一次性建檔 → 每次活動下拉選用）與建卡到啟用（7 步）。

## 二、規則一覽

| 流程 | 步驟 | 對應規格頁 |
|---|---|---|
| 型錄複用 | ① 建檔型錄 → ② 活動下拉選用 → ③ 只有缺規格時才新增卡 | 01-A · 01-C |
| 建卡到啟用 | ① 點新增 → ② 填名稱選星級 → ③ 勾遊戲 → ④ 填次數封頂 → ⑤ 設有效期 → ⑥ 二次確認 → ⑦ 確認後生效 | 01-B |

## 三、條列規則

- 畫布綁定鐵則：一張卡片只綁一個 h2 標題，標注編號＝h4 編號，一個流程一份文件。
- 本版定調：遊戲列表鎖在卡模板上；結算規則（上限／次數）一律鎖卡；入帳一律主錢包真金。
- 若未來遊戲列表常隨活動主題換，再把它升級為「活動可覆寫」，其餘規則仍鎖卡。

## 四、詳細說明（DEV V1.15 原文，一字未改）

### 本頁操作流程 · 型錄複用（直式）

一次性. **① 建檔型錄** — 獎勵清單建好 R_FS_1~5STAR（各幣值 bet／遊戲列表／上限）

每次活動. **② 下拉選用** — 抽寶箱獎項欄位選現成 reward_id（如第 8 抽 ← R_FS_3STAR）

僅缺規格時. **③ 才新增卡** — 現有卡都不符（特殊次數／遊戲／上限）才回型錄多建一張，之後一樣複用

> **遊戲列表會不會常變？（已定調）**　這是唯一可能逼你常常新建卡的欄位：  · 列表大致固定 → **遊戲列表也鎖在卡模板上**，純複用最單純（*本版採此*）  · 列表常隨活動主題換 → 未來把**遊戲列表升級為「活動可覆寫」**，其他結算規則（上限 / 次數）仍永遠鎖在卡上 **本版定調**：遊戲列表鎖卡模板；結算規則一律鎖卡；入帳一律主錢包真金，無錢包別設定。

### 本頁操作流程 · 建卡到啟用（直式）

01. **點「＋新增發放卡」** — 自列表頁進入；**此時尚未產生 reward_id**，表單上看不到 ID 欄位V1.14 改

02. **填名稱、選星級** — 名稱僅營運辨識；星級 bet 唯讀帶出各幣值每轉押注

03. **勾選可用遊戲** — 最多 8 款，即時顯示「已選 n / 8」

04. **填次數＋封頂倍數** — face_value 與各幣值 max_conversion 自動算出（唯讀）

05. **設定有效期** — 發卡 +N 天（預設）；自然日 GMT+8

06. **按「啟用」→ 二次確認** — 彈窗摘要星級／次數／有效期／上限／遊戲數；缺漏時確認鈕不可按

07. **確認後生效** — **系統於此刻產生 reward_id**；卡片進入型錄，可被抽寶箱獎項設定與活動引用V1.14 改

## 相關頁面

- 🔗 00 · 母計畫 Hub（模組索引在該頁）
